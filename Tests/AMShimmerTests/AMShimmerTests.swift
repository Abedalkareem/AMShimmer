    import XCTest
    @testable import AMShimmer

    final class AMShimmerTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(AMShimmer().text, "Hello, World!")
        }
    }
